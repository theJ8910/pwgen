from pwgen import VERSION

from setuptools import setup

setup(
    name        = "pwgen",
    version     = VERSION,
    author      = "theJ89",
    description = "theJ89's Password Generator",
    packages    = [ "pwgen" ],
    zip_safe    = True
)